global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h
  receiver: 'email'
  routes:
  - match:
      severity: critical
    receiver: 'email'

receivers:
- name: 'email'
  email_configs:
  - to: 'example@qq.com'
    from: 'example@qq.com'
    smarthost: 'smtp.qq.com:587'
    auth_username: 'example@qq.com'
    auth_password: 'xxx'
    auth_identity: 'example@qq.com'
    require_tls: true

inhibit_rules:
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname', 'instance']